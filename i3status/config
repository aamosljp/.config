general {
    colors = true
    interval = 5
}

order += "memory"
order += "disk /"
order += "load"
order += "tztime local"

tztime local {
    format = "%Y-%m-%d %H:%M:%S"
}

load {
    format = "%1min"
}

disk "/" {
    format = "%avail"
}

memory {
    format = "%used / %total"
}
